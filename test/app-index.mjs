import test from 'node:test'
import assert from 'node:assert'

test('env', () => {
  assert(true)
  // can get /
  // can initDB
  // can get /users
  // can post /users
  // can 'put' /users/:id
  // can 'delete' /users/:id
})
